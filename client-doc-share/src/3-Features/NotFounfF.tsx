import SectionTitle from "../4-Widgets/SectionTitle/SectionTitle";

export default function NotFoundF() {
  return (
    <div>
      <SectionTitle title="Страница не найдена" />
      <p>Страницу по указанному uri не найдена.</p>
    </div>
  );
}
